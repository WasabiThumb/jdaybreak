# https://docs.gradle.org/9.6.0/userguide/performance.html
org.gradle.parallel=true
org.gradle.tooling.parallel=true
org.gradle.daemon=true
org.gradle.caching=true

# Unfortunately the configuration cache breaks on MacOS,
# feel free to set this in your local gradle.properties
# if you are not on MacOS.
# org.gradle.configuration-cache=true
